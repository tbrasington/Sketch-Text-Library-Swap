{
  "name": "text-library-swap",
  "description": "",
  "version": "0.1.1",
  "engines": {
    "sketch": ">=49.0"
  },
  "skpm": {
    "name": "text-library-swap",
    "manifest": "src/manifest.json",
    "main": "text-library-swap.sketchplugin",
    "assets": [
      "assets/**/*"
    ],
    "sketch-assets-file": "sketch-assets/icons.sketch"
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.6.0"
  },
  "author": "Thomas Brasington <Thomas.Brasington@net-a-porter.com>",
  "dependencies": {
    "fuzzysort": "^1.1.4"
  }
}
